[project]
name = "readability-training"
version = "0.1.0"
description = "Training tools for readability-cli logistic regression model"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.20",
    "scikit-learn>=1.0",
]

[tool.uv]
dev-dependencies = []
